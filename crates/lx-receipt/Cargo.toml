# SPDX-License-Identifier: Apache-2.0
# Tier T0 — see LICENSING.md (that table is NORMATIVE) and DECISIONS.md
[package]
name = "lx-receipt"
description = "Receipt schema types, canonical JSON (JCS), validation, amendment chain."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license                = "Apache-2.0"
repository.workspace   = true

[dependencies]
lx-types = { workspace = true }

[lints]
workspace = true
