//! SPDX-License-Identifier: Apache-2.0
//!
//! `lx-verify` — the standalone verifier binary.
//!
//! Ships separately from `lx` so that verification can be run by someone who has
//! not installed the engine, and audited by someone who will not accept a Fair
//! Source dependency. All logic lives in the library half of this crate.

fn main() {
    // TODO: implement. See docs/cli.md for the command surface.
    unimplemented!("lx-verify")
}
