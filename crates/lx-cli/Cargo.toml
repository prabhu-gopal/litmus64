# SPDX-License-Identifier: FSL-1.1-ALv2
# Tier T1 — see LICENSING.md (that table is NORMATIVE) and DECISIONS.md
[package]
name = "lx-cli"
description = "The `lx` binary (clap). Thin: all logic lives in lx-engine."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license                = "FSL-1.1-ALv2"
repository.workspace   = true

[dependencies]
lx-engine = { workspace = true }
lx-render = { workspace = true }
lx-verify = { workspace = true }

[lints]
workspace = true
