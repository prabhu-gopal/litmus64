# Receipt schema — generated

This directory is populated by `cargo xtask schema-gen` from the `lx-receipt` types.
CI (`xtask schema-check`) fails if the committed schema and the Rust types disagree, so the
schema can never drift from the code.
