---
name: False positive
about: Litmus64 reported a violation that was not real. THE most valuable report you can send.
labels: false-positive, corpus
---

**Thank you.** False positives are the category's cause of death, so this report is more valuable to
us than a feature request. It also becomes a permanent corpus case in `bench/corpora/`.

- Repo / PR (or a minimal reproducer):
- `lx --version`, profile used (`check` / `--deep` / `--read-only`):
- The obligation and evidence IDs (`lx explain OB-n`):
- Receipt (attach `.litmus64/receipts/<sha>.json` if you can share it):
- Why it was not real:
