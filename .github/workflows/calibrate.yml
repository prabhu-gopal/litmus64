# calibrate — see CODEBASE.md §10 for the full step list.
name: calibrate
on: { workflow_dispatch: {} }
jobs:
  todo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO: implement calibrate"
