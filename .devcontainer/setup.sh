#!/usr/bin/env bash
# One command, no README archaeology. If this fails first try, that is a P1 bug.
set -euo pipefail
echo "TODO: rust toolchain, tree-sitter grammars, python/node fixtures, sandbox probe"
echo "Then: cargo xtask check && cargo run -p lx-cli -- demo"
